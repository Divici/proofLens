# Personal study / reference material — not project documentation
SKILLS_AUDIT.md
CONDUCTOR_DESIGN.md
STUDY_GUIDE.md
PRIOR_ART*.md

# Conductor working docs — not project documentation
ALIGNMENT.md
CONDUCTOR_STATE.md
architecture-audit.md
.forge/

# Working memory + slice plans + research docs — not part of the
# deliverable. The brief asks for "brief documentation of approach,
# tools used, assumptions made"; that lives in APPROACH.md. The rest
# is personal context for the developer's next session, not the
# reviewer's reading material.
memory-bank/
issues/
research-findings/
PRD.md
PRESEARCH.md
RESEARCH.md

# Local Claude Code overrides
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Tesseract.js runtime-downloaded language data (root cwd cache).
# We bundle a copy explicitly under public/tessdata/eng.traineddata so the
# Vercel function loads it from disk instead of the projectnaptha.com CDN.
/eng.traineddata

# Git worktrees for isolated slice work
.worktrees/

# Environment / secrets
.env
.env.local
.env.*.local
.env.development
.env.production
*.pem

# Node / package managers
node_modules/
.pnp
.pnp.*
.yarn/install-state.gz

# Build outputs
.next/
out/
build/
dist/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
vitest.log
playwright.log
dev.log
eval.log

# Local smoke / probe scripts (one-off, not part of the app)
smoke.ps1
health.ps1
poll-health.ps1
deploy-smoke.ps1
warm-then-post.ps1
inspect-headers.ps1
hit-diagnose.ps1
check-tessdata.ps1

# Local crash dumps
*.stackdump

# Test / coverage
coverage/
.nyc_output/
test-results/
playwright-report/
playwright/.cache/

# Local-only generated content (stub-batch output, ad-hoc fixtures, etc.)
tmp/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Sentry
.sentryclirc
.env.sentry-build-plugin
